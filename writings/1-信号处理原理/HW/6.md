# 信号处理原理 第 6 次作业 #

李祥泽 2018011331

## 1 ##

对于信号 $f(t)$, 用间隔 $T$ 的冲激串采样, 得到采样信号: 
$$
\hat f(t)=\sum_{k\in\Z}{f(t)\delta(t-kT)}=\sum_{k\in\Z}{f(kT)}\delta(t-kT)
$$
对 $\hat f$ 作 FT, 利用定义式得到:
$$
\begin{align}
\mathcal{F}[\hat{f}(t)]
&= \int_{-\infty}^{+\infty}{
    \left( \sum_{k\in\Z}{f(kT)\delta(t-kT)} \right)
    e^{-j\omega t} 
\text{d}t} \\
&= \sum_{k\in\Z}{\left[
    f(kT)\cdot 
    \int_{-\infty}^{\infty}{
        \delta(t-kT)e^{-j\omega t}
    \text{d}t}
\right]} \\
&= \sum_{k\in\Z}{f(kT)e^{-j\omega kT}}
\end{align}
$$

利用 FT 的卷积性质和冲激函数的搬移特性又可以得到
$$
\mathcal{F}[\hat{f}]=\frac{1}{T}\sum_{k\in\Z}{F(\omega-k\omega_0)}
$$

以上两个式子相等, 再代入 $\omega=0$, 得到: 
$$
\sum_{k\in\Z}f(kT) = \frac1T \sum_{k\in\Z}{F(-k\omega_0)}
$$
等号两侧的 $k$ 互相无关, 用 $n=-k$ 代换右边, 即得到所求式: 
$$
T\cdot \sum_{k\in\Z}f(kT) = \sum_{n\in\Z}{F(n\omega_0)}
$$


## 2 ##

### (a) ###

$$
\begin{align}
DTFT[x(n)*x^*(-n)]
&= DTFT[x(n)] \cdot DTFT[x^*(-n)] \\
&= X(\omega) \cdot X^*(\omega)
\end{align}
$$

### (b) ###

$$
\begin{align}
DTFT[x(2n+1)] 
&= \sum_{n=-\infty}^{\infty}{x(2n+1)e^{-jn\omega}} \\
&(\text{let } m=2n+1 \text{, then } m \text{ ranges from } -\infty \text{ to } +\infty \\
&(\text{BUT } m \text{ can only have ODD values}) \\
&= \sum_{m=-\infty}^{\infty}{ \frac{1+(-1)^{m-1}}{2} \cdot x(m)e^{-j(\frac{m-1}{2})\omega}}\\
&= e^{\frac12j\omega}\left[X(\frac12 \omega) - X(\frac12 \omega + \pi)\right]
\end{align}
$$

### (c) ###

$$
\begin{align}
DTFT[x(n)-x(n-2)]
&= DTFT[x(n)]-DTFT[x(n-2)]\\
&= X(\omega) - e^{-2j\omega}\cdot X(\omega)\\
&= (1-e^{-2j\omega})X(\omega)
\end{align}
$$

### (d) ###

$$
\begin{align}
DTFT[x(n)*x(n-1)]
&= DTFT[x(n)] \cdot DTFT[x(n-1)]\\
&= X(\omega) \cdot e^{-j\omega}X(\omega)\\
&= e^{-j\omega}X^2(\omega)
\end{align}
$$

## 3 ##

$$
\begin{align}
Y(\omega)
&= DTFT[y(n)]\\
&= \sum_{n=-\infty}^{\infty}{y(n)e^{-jn\omega}}\\
&= \sum_{n=kL}{x(n/L)e^{-jn\omega}}\quad,k\in\Z \\
&(\text{let }m = n/L)\\ 
&= \sum_{m=-\infty}^{\infty}{x(m)e^{-jLm\omega}}\\
&= X(L\omega)
\end{align}
$$

